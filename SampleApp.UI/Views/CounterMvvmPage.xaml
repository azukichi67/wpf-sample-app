<Page x:Class="SampleApp.UI.Views.CounterMvvmPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SampleApp.UI.Views" 
      xmlns:viewmodels="clr-namespace:SampleApp.UI.ViewModels" 
      d:DataContext="{d:DesignInstance Type=viewmodels:CouterMvvmViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CounterMvvmPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="カウンター (MVVM)" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="-" Command="{Binding DecrementCommand}" Margin="10" Width="50"/>
                <TextBlock Text="{Binding Count}" FontSize="20" Margin="10" VerticalAlignment="Center"/>
                <Button Content="+" Command="{Binding IncrementCommand}" Margin="10" Width="50"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
